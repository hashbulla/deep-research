# Context log — deep-research

> Dated session summaries. Newest entries appended at the bottom.

## 2026-06-20 — AI-178 newsletter corpus ingestion: brainstorm + architecture research

Brainstormed and challenged Linear AI-178 (Urgent: "use Unipile to retrieve the Telegram newsletter-agent feed to archive and index"). Reframed it as the producer half of the already-shipped newsletter-signal source (consumer AI-41 shipped; the corpus is the gap — `~/.claude/deep-research/newsletter-corpus/` is absent, so the consumer degrades to `corpus_present:false` every run). Established the producer is owned (`~/Desktop/AIEngineering/Agents/newsletter-watch-agent/`), the feed is a private Telegram channel, Unipile is the user's chosen pull tool, and the producer's "step 12" briefs-corpus emit is specced but unbuilt (30-day janitor means Telegram is the only full history). Ran `/deep-research` (standard) to settle the retrieval engine: verdict is keep FTS5/BM25 + recency — RAG/CAG/hybrid each lose on a cited criterion and none fits the consumer's zero-network/zero-LLM I4a contract. Artifacts at `~/Desktop/AIEngineering/research-runs/ai-178-newsletter-corpus-retrieval/` (gate FAIL on 2 calibration gates by design of a narrow question; documented). Ended at the design-approval gate — `/no-loss` invoked before the next-step fork was answered. Checkpoint: `.claude/no-loss/latest.md`.

## 2026-06-23 — AI-182 Langfuse observability/eval: scoping + seeding research + gotcha fix

Created Linear **AI-182** (stand up Langfuse Cloud Hobby observability + live eval for `/deep-research`, as a harness-engineering / agent-eval upskilling exercise; related AI-38 learning + AI-124 offline harness). Settled the architecture: Path A (live tracing via an OTEL **Collector**) first, Path B (offline artifact→Datasets/Scores uploader) in README roadmap, Path C (SDK port) out — driven by the markdown-only / zero-network skill invariant (I4a). Ran an exhaustive `/deep-research` on the topic (gate **PASS**, 103 sources) at `research-runs/langfuse-agent-eval/`; key finding: Claude Code emits OTEL metrics+log events (traces **beta**) but Langfuse OTLP ingests **traces-only** → a Collector is required (also the redaction egress gate for the content-traced-but-sanitized requirement). Found + fixed a real skill gotcha — `newsletter_search.py` rejected `--since YYYY-MM` while the skill documents `YYYY`/`YYYY-MM-DD`; broadened `parse_date`, added a test, logged it, committed on branch `fix/newsletter-since-partial-dates` (`a3a7203`). Blocked on the user creating the Langfuse account to wire Path A Milestone 1. Checkpoint: `.claude/no-loss/latest.md`.

## 2026-06-25 — AI-182 Langfuse Slice-1: Collector egress gate built + redaction proven end-to-end

Resumed AI-182. Account was live (keys in `~/second-brain/.secrets/langfuse.env`, EU region); standing instruction set — all Langfuse context persists to `~/second-brain`. Build mode: guided. Moved the stranded AI-182 docs off the AI-183 branch onto a clean `feat/langfuse-observability` off current `origin/main` (AI-183 already merged there). Built the lab tooling under `eval/langfuse/` (outside the markdown-only skill surface, per I4a): an OTEL Collector (contrib 0.155) egress gate — `otlp` → `memory_limiter` + `redaction` + `transform`/OTTL + fail-closed `filter` + `batch` → `otlp_http/langfuse` (protobuf) + `debug` — plus `run-collector.sh` (sources the second-brain secret, never prints it), `enable-claude-telemetry.sh` (sourced), `redaction-proof.sh`. Booted clean (0/0), proved redaction **server-side**: a seeded canari masked in the real stored Langfuse trace (verified via the public API), then purged. Caught + fixed a real tail-leak (uppercase-only regex left `****f3a9b` → `(?i)`); instrumented the Collector's own `:8888` metrics to confirm egress (`sent_spans`, 0 `send_failed`) since the Hobby UI lags ~30-60s. Persisted findings to `~/second-brain` (`50-stack/langfuse.md` failure modes + `LANGFUSE_HOST`→`LANGFUSE_BASE_URL` drift fix; decision note in `20-engineering/`). Posted the Linear status update; stayed In Progress. Open: user runs a real /deep-research in a telemetry-enabled session (M1 confirmation of Claude Code's beta trace export); Milestone 2 = content/cost log→span enrichment. Repo + second-brain changes uncommitted pending user OK.
